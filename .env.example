# Copy to .env and adjust. Never commit the real .env.
DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=*

# Host ports used by local development and tests. Change them if another
# PostgreSQL or Redis already listens on the defaults.
HOST_POSTGRES_PORT=5432
HOST_REDIS_PORT=6379

# Postgres (one container, one database per service)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_SUPERUSER=postgres
POSTGRES_SUPERUSER_PASSWORD=change-me
DB_PASSWORD=change-me

# Redis
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1

# RabbitMQ
RABBITMQ_USER=marketplace
RABBITMQ_PASSWORD=change-me
RABBITMQ_URL=amqp://marketplace:change-me@rabbitmq:5672/

# Object storage (S3 compatible)
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_ENDPOINT=http://minio.localhost
S3_ACCESS_KEY=marketplace
S3_SECRET_KEY=change-me
S3_BUCKET=marketplace
S3_REGION=us-east-1
IMAGE_MAX_UPLOAD_BYTES=10485760
IMAGE_UPLOAD_URL_TTL_SECONDS=600

# Search
ELASTICSEARCH_URL=http://elasticsearch:9200

# Auth
PHONE_ALLOWED_REGIONS=UZ
JWT_PRIVATE_KEY_PATH=/run/secrets/jwt_private.pem
JWT_PUBLIC_KEY_PATH=/run/secrets/jwt_public.pem
ACCESS_TOKEN_LIFETIME_MINUTES=15
REFRESH_TOKEN_LIFETIME_DAYS=30

# SMS / notifications
SMS_BACKEND=console
ESKIZ_EMAIL=
ESKIZ_PASSWORD=
TELEGRAM_BOT_TOKEN=
SMTP_HOST=mailpit
SMTP_PORT=1025

# Payments
PAYMENT_MOCK_ENABLED=True
PAYME_MERCHANT_ID=
PAYME_KEY=
CLICK_MERCHANT_ID=
CLICK_SERVICE_ID=
CLICK_SECRET_KEY=
